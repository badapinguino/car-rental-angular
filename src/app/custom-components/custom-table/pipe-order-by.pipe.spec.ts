import { PipeOrderByPipe } from './pipe-order-by.pipe';

describe('PipeOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
